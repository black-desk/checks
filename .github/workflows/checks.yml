on: [pull_request]

jobs:
  test-checks:
    permissions:
      checks: write
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    name: Checks
    steps:
      - uses: black-desk/checks@v0.1.11
