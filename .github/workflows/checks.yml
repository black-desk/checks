on: [pull_request]

jobs:
  test-checks:
    permissions:
      checks: write
      contents: read
      pull-requests: write
    runs-on: ubuntu-latest
    name: Checks
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: recursive
      - uses: ./
